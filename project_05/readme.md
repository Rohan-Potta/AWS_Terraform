In this project we will be using lambda function for the image processing.

When we place an image in teh bucket a s3 notification is sent to lambda and then will make 5 copies with different file types and compression/quality

1. 